# packages/security

Shared security utilities for QuantumFintek (JWT helpers, password hashing, audit event schemas).

Currently the primary implementation lives under `apps/api/app/identity/security/`.
This package will absorb pure, reusable logic as the platform grows.
